#include "main.h"
/**
 *_islower - print lower if it return to 1
 *@c: int value shold be a lower alpha
 *Return: 1 if it lower otherwise 0
 */
int _islower(int c)
{
if (c == 97; c <= 122; c++)
{
return (1);
}
else
{
return (0);
}
}
