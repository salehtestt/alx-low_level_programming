#include "main.h"
/**
 *_abs - print the absolute value of int
 *@n: the int that i have to find the ans for
 *Return: n if it more or equal 0 and -n if it less thsn 0
 */
int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n == 0)
{
return (n);
}
else
{
return (-n);
}
}
